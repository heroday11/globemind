# 安全政策

GlobeMind 处理新闻资料、研究结果、身份信息和模型/数据库连接。安全问题请按最小披露原则处理；不要在公开 issue、提交、聊天记录或日志中粘贴密码、token、API key、数据库 URL、个人数据、生产数据或可复现攻击所需的敏感细节。

## 报告安全问题

请使用项目维护者或所属组织批准的私密安全渠道提交报告；如果组织尚未指定渠道，先联系仓库负责人确定接收人，再发送最小化报告。不要把敏感漏洞直接发到公开讨论区。报告应尽量包含：

- 受影响的源码路径、版本或提交；
- 不包含秘密的复现步骤和影响范围；
- 触发条件、权限要求和潜在数据影响；
- 已采取的临时缓解措施。

请只测试你明确获授权的本地或候选环境，不要扫描、攻击或修改生产服务、真实用户数据、第三方数据源或运行中的长管线。项目不在此文件中承诺未经授权或未确认的响应时限；接收和处置由维护者依据严重性、影响和可用资源决定。

## 开发者安全要求

- 使用环境变量或受控 secret 文件；提交前检查 diff，确认没有凭据、`.env`、导出数据、日志和模型密钥。
- API、数据库和外部连接器应 fail closed：缺配置、过期、低覆盖或证据不足时返回明确的不可用/不可计算状态，不伪造结果。
- 遵守最小权限、参数化查询、认证/授权边界、请求大小/速率限制和日志脱敏约束。
- 不要把生产 release 当作开发目录，不要运行或导入 release/`previous`/`rejected` 中的 Python，也不要把它们加入 `PYTHONPATH`。
- Python 诊断必须在启动前设置 `PYTHONDONTWRITEBYTECODE=1` 并使用 `python -B`；不要输出进程参数、环境变量、数据库 URL、token 或 secret 内容。
- 依赖升级要核对锁定版本、许可证、供应链来源和测试影响；安全修复应附最小可复核证据。

更详细的运行时和发布约束见 [`AGENTS.md`](AGENTS.md)、[`docs/operations/RUNTIME_CONTROL.md`](docs/operations/RUNTIME_CONTROL.md) 和 [`docs/operations/RELEASES.md`](docs/operations/RELEASES.md)。
