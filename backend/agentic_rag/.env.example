# 复制�?.env 后填写密�?LLM_BACKEND=cloud_api
CLOUD_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
CLOUD_API_KEY=
CLOUD_API_MODEL=qwen-plus

# ---- 命名 / Stage5 LLM：云端优�?+ Docker �?vLLM 安全回退（推荐）----
# 流水线用百炼等云�?OpenAI 兼容 API；遇内容安全拦截时改连本�?Docker 映射�?vLLM（勿在宿主机 pip �?vllm）�?# QWEN_BACKEND=openai_api
# QWEN_LOCAL_FALLBACK_ON_SAFETY=1
# 显式指定 Docker vLLM（与容器 8000:8000 一致；模型名须与容�?--served-model-name 一致）
# QWEN_LOCAL_FALLBACK_BASE_URL=http://127.0.0.1:8000/v1
# QWEN_LOCAL_FALLBACK_MODEL=/model/Qwen2.5-7B-Instruct-AWQ
# 若未�?QWEN_LOCAL_FALLBACK_*，则回退使用下面 QWEN_BASE_URL / QWEN_MODEL_NAME（须�?curl http://127.0.0.1:8000/v1/models 一致）
# QWEN_BASE_URL=http://127.0.0.1:8000/v1
# QWEN_MODEL_NAME=/model/Qwen2.5-7B-Instruct-AWQ
# 仅全走本�?Docker、不用云端时: QWEN_BACKEND=vllm（同�?URL�?
# ---- analysis_service 等若单独�?vLLM，可�?Docker 对齐 ----
# VLLM_BASE_URL=http://127.0.0.1:8000
# VLLM_MODEL=Qwen2.5-7B-Instruct

# ---- Milvus（与项目�?docker-compose.yml 一致；未起容器时会连失败）----
# 启动: 在仓库根目录执行  docker compose up -d
# 默认映射: 宿主�?19530=gRPC, 9091=HTTP 健康检查（勿与 PG 54333 / vLLM 8000 / MCP 8765 混淆�?MILVUS_HOST=localhost
MILVUS_PORT=19530
# 阶段44 DB→Milvus�?=仅同步涉华；0=同步「标题非空」全库（全局向量，与 config.yaml milvus_sync_china_only 一致；环境变量优先�?MILVUS_SYNC_CHINA_ONLY=0

# ---- TEI: GPU embedding service (optional). See deploy/tei/docker-compose.yml ----
# BGE_TEI_URL=http://127.0.0.1:8080
# BGE_TEI_DIM=1024
# BGE_TEI_BATCH_SIZE=64
# BGE_TEI_READ_TIMEOUT=600
# BGE_TEI_L2_NORMALIZE=0
